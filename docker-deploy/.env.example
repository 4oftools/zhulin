# 复制为 .env 后按需修改（docker compose 会自动读取同目录下的 .env）
# cp .env.example .env

# MySQL
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=zhulin

# 映射到宿主机端口（MySQL 默认不映射，见 docker-compose.yml 说明）
BACKEND_PORT=8080
FRONTEND_PORT=80

# 是否在空库时加载演示数据（true/false）；生产建议 false
ZHULIN_DEMO_DATA=true

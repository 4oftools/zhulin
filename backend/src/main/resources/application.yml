zhulin:
  # 空库时自动写入演示数据；生产环境请改为 false
  demo-data: true

spring:
  application:
    name: zhulin-backend
  profiles:
    active: mysql # Change to 'postgresql' to use PostgreSQL
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080
